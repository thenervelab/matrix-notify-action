# matrix-notify-action
Post end-to-end encrypted messages to a Matrix room from CI. A small matrix-rust-sdk CLI plus a GitHub Action: the message is encrypted before it leaves your runner; the homeserver never sees it.
